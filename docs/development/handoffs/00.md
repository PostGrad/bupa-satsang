Task: 00 - Checkpoint and finish the existing web/API scaffold
Base: f5721466289dfa5adac56ede137ba390f6270518
Result: ready for review

Files:
- package.json - kept root test boundaries explicit so Vitest unit runs do not load Playwright e2e specs.
- apps/client/e2e/shell.spec.ts - added fresh-start storage reset and explicit selected-radio aria-checked assertion.
- apps/api/test/health-live.test.ts - added real HTTP regression for GET /health/live returning {status:"ok"}.
- docs/superpowers/plans/2026-09-06-kitchen-development/progress.md - marked task 00 ready for review with evidence.
- docs/development/handoffs/00.md - this execution handoff.

Tests:
- pnpm install --frozen-lockfile
  - First non-TTY attempt failed with ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY.
  - TTY attempt accepted pnpm's reinstall prompt, then failed inside the sandbox because registry DNS was blocked after node_modules was removed.
  - Reran the same frozen install outside the sandbox: passed; lockfile was already up to date and dependencies were restored.
- pnpm lint
  - Passed. Nx reported sandbox socket warnings and fell back to in-process plugin execution.
- pnpm typecheck
  - Passed. Nx reported sandbox socket warnings and fell back to in-process plugin execution.
- pnpm test:unit
  - Failed before the package.json fix because Vitest loaded apps/client/e2e/shell.spec.ts and Playwright rejected test() under Vitest.
  - Passed after excluding apps/client/e2e/**/*.ts from the unit script: 2 files, 2 tests.
- pnpm test:api
  - Failed inside the sandbox because tsx could not create its IPC pipe.
  - Passed outside the sandbox: 1 file, 1 test.
- pnpm test:web apps/client/e2e/shell.spec.ts
  - Timed out inside the sandbox waiting for Expo web server.
  - Passed outside the sandbox after rerun: 1 test.
- pnpm exec nx run client:web-build
  - Passed. Nx reported sandbox socket warnings; Expo exported dist successfully.

Manual evidence:
- Environment: macOS local checkout at /Users/postgrad/Github/BUPA-Satsang on 2026-09-06, branch develop.
- Current base before task: f5721466289dfa5adac56ede137ba390f6270518.
- Inspected the named manifests, Nx targets, Expo shell files, Nest health endpoint, CI workflow and version matrix.
- Confirmed CI does not run Android/iOS compilation. Native Android compilation and physical-device acceptance remain pending and were not run.

Contract changes: none.

Open defects:
- None blocking task 00. The local Codex sandbox cannot run some local-server/IPC checks without escalation; this is environmental and recorded above.

Next: task 01, after task 00 review/acceptance.
